{
  "name": "zhuobu-bullet",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild client/main.ts worker/main.ts --target=es2015 --platform=node --outdir=dist --bundle --format=esm",
    "dev": "esbuild --sourcemap client/main.ts worker/main.ts --outdir=dist --platform=node  --bundle --format=esm --watch  --servedir=.",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ogl": "^1.0.7"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "minigame-api-typings": "^3.8.4"
  }
}
